{
  "name": "space_invaders",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -d index.js -o build/bundle.js &> browserify.log"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "d3": "^3.5.5"
  }
}
